# Smash Track

这是一个基于 React + Vite 的羽毛球比赛记录应用，已经改为连接 Supabase 的后端数据层。

## Supabase 配置

- URL: https://keurapvlrfqdehouprrp.supabase.co
- API Key: sb_publishable_UKk9dESqUDvRVpF7bYOGIA_n8LPsNFt

默认已配置环境变量：

- VITE_SUPABASE_URL
- VITE_SUPABASE_ANON_KEY

## 本地启动

1. 安装依赖
   npm install
2. 启动开发服务器
   npm run dev

## 创建 Supabase 表

在 Supabase SQL Editor 中执行：

```sql
create table if not exists players (
  id bigint generated by default as identity primary key,
  nickname text not null,
  handle text,
  avatar text,
  level text default '入门',
  wins integer default 0,
  games integer default 0,
  password_hash text,
  created_at timestamptz default now()
);

create table if not exists matches (
  id bigint generated by default as identity primary key,
  date date not null,
  court text,
  winner bigint not null,
  loser bigint not null,
  winner_score integer not null,
  loser_score integer not null,
  created_at timestamptz default now()
);

insert into players (nickname, handle, avatar, level, wins, games, password_hash)
values
  ('林默', 'linmo', '林', '进阶', 18, 25, ''),
  ('周予安', 'yuan', '周', '高手', 15, 22, ''),
  ('沈清禾', 'qinghe', '沈', '进阶', 12, 19, ''),
  ('陈放', 'chenfang', '陈', '入门', 9, 18, '')
on conflict do nothing;

insert into matches (date, court, winner, loser, winner_score, loser_score)
values
  ('2026-08-24', '东区 03 号场', 1, 2, 21, 17),
  ('2026-08-22', '东区 01 号场', 3, 4, 21, 14),
  ('2026-08-20', '中央球馆', 2, 4, 21, 12)
on conflict do nothing;
```

## 关键说明

- 前端通过 Supabase SDK 自动访问 REST API，不需要自己拼接 /rest/v1/
- 这里使用的是匿名 publishable key，适合公开数据展示
- 若要开启真实登录，需要再开通 Supabase Auth，并切换到安全的 session 流程
